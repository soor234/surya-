import java.io.*;
import java.util.*;
public static void main(string  args[])
{
 public class  string
 {
     int t;
     scanner scn= new scanner(scn);
     int sc = new  sc(int)
     while(sc.readline())
     {
         int n,k;
         {
 buffered  reader = new  buffered reader(sc);
 {
     s.nextline();
     {
         while(s.parseline())
         {
             integer parse int= new integer parse int(sc)
             {
                 int.sc formatline(n,k)
                 {
                     integer.parse line()
                     {
                         
                     }
                 }
             }
         }
     }
 }
         }
     }
     
 }
}